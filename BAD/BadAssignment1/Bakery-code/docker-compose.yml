version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.WEB
    environment:
      - API_URL=http://api:80
    ports:
      - "5050:80"

  api:
    build:
      context: .
      dockerfile: Dockerfile.API
    ports:
      - "6060:80"
